apiVersion: v1
kind: Service
metadata:
  name: http-load-balancer
spec:
  type: NodePort
  selector:
    app: http-load-balancer
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
