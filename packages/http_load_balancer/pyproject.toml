[project]
name = "http_load_balancer"
version = "1.0.0"
description = ""
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "loguru>=0.7.3",
    "pydantic>=2.13.4",
    "pydantic-settings>=2.14.2",
    "PyYAML>=6.0.2",
]

[project.scripts]
http_load_balancer = "http_load_balancer.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
