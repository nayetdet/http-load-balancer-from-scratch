[project]
name = "sample-app"
version = "1.0.0"
description = ""
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "gunicorn>=26.0.0",
    "pydantic-settings>=2.14.2",
]

[project.scripts]
sample_app = "sample_app.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
